#include<bits/stdc++.h>
#include <cstdio>

using namespace std;

int main()
{
    int a;
    long int b;
    char s;
    float x;
    double y;

    scanf("%d %ld %c %f %lf", &a, &b, &s, &x, &y);
    printf("%d\n%ld\n%c\n%f\n%lf\n", a, b, s, x, y);
    return 0;
}
